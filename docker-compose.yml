version: "3.9"

services:
  bot:
    build:
      context: .
      dockerfile: Dockerfile
    env_file:
      - .env
    volumes:
      # Persist SQLite and downloaded files across restarts locally
      - ./data:/app/data
    restart: unless-stopped
